// @flow
import React from 'react';
import { LockedWrapper } from '../style';

const LockedMessages = ({ children }) => (
  <LockedWrapper>{children}</LockedWrapper>
);

export default LockedMessages;
