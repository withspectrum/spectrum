import React from 'react';
import { LoadingInboxThread } from '../../../components/loading';

export default props => (
  <div>
    <LoadingInboxThread />
    <LoadingInboxThread />
    <LoadingInboxThread />
    <LoadingInboxThread />
    <LoadingInboxThread />
    <LoadingInboxThread />
    <LoadingInboxThread />
    <LoadingInboxThread />
    <LoadingInboxThread />
    <LoadingInboxThread />
  </div>
);
