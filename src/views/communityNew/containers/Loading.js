// @flow
import React from 'react';
import { Loading } from 'src/components/loading';

export const LoadingView = () => {
  return (
    <React.Fragment>
      <Loading />
    </React.Fragment>
  );
};
