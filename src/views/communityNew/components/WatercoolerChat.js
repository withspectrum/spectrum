// @flow
import * as React from 'react';
import { WatercoolerWrapper } from '../style';

export const WatercoolerChat = (props: Props) => {
  const { id } = props;

  return <WatercoolerWrapper />;
};
