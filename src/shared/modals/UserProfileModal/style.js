import styled from 'styled-components';

export const Username = styled.p`
  font-size: 14px;
  font-weight: 500;
  color: ${({ theme }) => theme.text.alt};
  margin-bottom: 24px;
  line-height: 1.4;
`;
